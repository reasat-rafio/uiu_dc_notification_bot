type ScrapEventData {
  title: String
  slug: String
  content: String
  vanue: String
  date: String
}

type Query {
  scrapingEvent: ScrapEventData
}

type Mutation {
  scrapeEvent: ScrapEventData
}
