type ScrapNoticeData {
  title: String
  slug: String
  content: String
}

input UpdateScrapingInput {
  id: Int!
}

type Query {
  scrapingNotice: ScrapNoticeData
}

type Mutation {
  scrapeNotice: ScrapNoticeData
}
